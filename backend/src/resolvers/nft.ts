/*
import { ethersClient } from '../utils/blockchain';

export async function getNFT(_parent: any, args: { id: string }) {
    const nftData = await ethersClient.getNFTDetails(args.id);
    return JSON.stringify(nftData);
}

export async function mintNFT(_parent: any, args: { recipient: string }) {
    const tx = await ethersClient.mintNFT(args.recipient);
    return tx.hash;
}*/
